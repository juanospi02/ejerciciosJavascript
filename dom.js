/*let nameImput = document.getElementById('nombre');
console.log(nameImput);


// el queryselector me trae culquier tipo de selector, puede ser etiqueta, puede ser id o etiqueta
let saludoP = document.querySelector('.mostrarSaludo');
console.log(saludoP);

// acceder al contenido de nuestras etiquetas

let name = nameImput.value;

// funciones- son bloques que encapsulan codigo y ejecutan acciones

function saludarUsuario(){
    //aca va el codigo que quiero ejecutar
    let name = nameImput.value;
    console.log(name)
    let mensaje = '';
    if(name){
        mensaje = 'hola' + name;
        //textcontent me permite cambiar el contenido dentro de etiquetas
        saludoP.textContent = mensaje;
    } else{
        mensaje = 'hola desconocido';
        saludoP.textContent = mensaje
    }


}*/